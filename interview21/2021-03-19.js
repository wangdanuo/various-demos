/**
 * 第一题
 * 说明：实现一个方法 compareVersion，用于比较 app 的两个版本号（version1、version2）
 *      若 version1 > version2，返回 1；
 *      若 version1 < version2，返回 -1；
 *      其他情况，返回 0；
 *      版本号规则 x.y.z，其中 xyz 均为正整数，至少有 n 位
 * 示例：
 * compareVersion('0.1', '1.1.1');  // 返回 -1
 * compareVersion('13.37', '1.2 '); // 返回 1
 * compareVersion('1.1', '1.1');    // 返回 0
 */
function compareVersion() {
  
}

/**
 * 第二题
 * 说明：实现一个转换函数 parseFunc，把一个 json 对象的 key 转换到小驼峰形式
 *      json 对象 key 默认全部由 a-z、A-Z、-、_  组成
 * 示例：
 * parseFunc({'a_b-cd': 1});  // 返回 {'aBCd': 1}
 */

 function parseFunc() {

}

/**
 * 第三题
 * 说明：实现一个自定义方法，输入一个未排序的数组 arr 和数字 N，返回此数组中第 N 个最大的元素
 *      默认 N 值始终在 arr 长度范围内，且数组内元素类型未知
 * 示例：
 * 输入: [3,2,1,5,6,4] 和 N = 2, 输出: 5
 * 输入: [3,2,3,1,2,4,5,5,6] 和 N = 4, 输出: 4
 */

function customDelayFunc(arr, N) {

}

/**
 * 第四题
 * 说明：实现一个自定义方法 customDelayFunc，内部利用 promise 实现一个延迟函数
 */

 function customDelayFunc() {

}


